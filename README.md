# 🚀 Amarendra’s Custom Node.js Server

A simple personalized Node.js HTTP server built **without Express**, using the built-in `http` module.  
It serves multiple pages — `/`, `/about`, and `/contact` — with basic HTML and CSS styling.

---

## 🧠 Features
- ✅ Built using pure **Node.js** (no external libraries)
- 🌐 Routes: `/`, `/about`, `/contact`
- 🎨 Inline CSS styling with a clean layout
- ⚙️ Lightweight and beginner-friendly

---

## 🧩 Project Structure
